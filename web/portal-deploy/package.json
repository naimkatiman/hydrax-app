{
  "name": "hydrax-portal-deploy",
  "version": "0.1.0",
  "private": true,
  "description": "Combined static-site deploy of all 5 Hydrax portals (demo mode).",
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
