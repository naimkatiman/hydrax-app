project: hydrax-app
updated: 2026-04-26T20:30:00+08:00
status: in_progress
summary: "Parallel-dispatch session closed three follow-ups end-to-end (audit emission, products list, disabled-state test) after a multi-agent collision on workflow-svc forced a recovery pass. Final state — issuer-portal /products list page wired through workflow-svc (List handler + repo.List + 6 httptest cases) → BFF (listProducts proxy + session-derived tenant) → api-client (useListProductsQuery) → React route at /products with row links to /products/:id. Audit emission on Transition wired through new internal/auditclient (98a688d) into Transition handler with derived 2s context, best-effort failure logging, recordingEmitter test double, 6 emit-related test cases, AUDIT_SVC_URL env var. Plus a vitest case guarding disabled={isLoading} on transition buttons. All per-service and per-workspace gates green: workflow-svc go vet/test (handlers package now 18 tests), pnpm -r typecheck/test/build clean across 12 workspaces (257 tests passing — bff 73, integration-svc 58, ui 40, issuer-portal 24, investor-portal 13, api-client 12, ops-console 11, distributor-portal 10, admin 8, tenant-theme 7, notify-svc 1, +1 vitest in @hydrax/issuer-portal for the disabled-state guard). The session's 6 commits — 304fecc, 98a688d, 3e30212, b474485, 53126ad, f042ffc, 03b8727 (Agent A/B output) plus fcf464a, 30cf7b2, 15b579e, 7b2f9c7 (recovery) — all single-concern, all conventional, ≤15 files. Audit emission deferred follow-up is now CLOSED."
current_focus:
  - Auth slice 2a (passkeys server substrate) implementation complete (5 commits d673e17 → 775fd87 → fc05924 → 3c45e90 plus docs commit). All 11 plan tasks executed; 58/58 integration-svc tests + 73/73 bff tests green; typecheck clean across both services. Manual browser smoke (Task 11 — the slice closure gate per the plan) is NOT yet performed and cannot be performed by the CLI agent. User must drive a real browser through register → auth → whoami round-trip before declaring slice 2a fully closed. Slice 2a does NOT enable production passkey enrollment (bootstrap requires AUTH_DEV_LOGIN=1, which is fail-closed in prod). Next decision after manual smoke: slice 2b (magic-link enrollment, real production bootstrap path) or slice 2d (portal UI for passkeys).
recently_verified:
  - pnpm -r --if-present typecheck clean across all 12 workspaces (3 web packages + 5 web apps + 3 TS services + integration-svc, all tsc -p tsconfig.json --noEmit)
  - pnpm -r --if-present test -- --run = 22 web tests passing (5 tenant-theme + 5 ui + 2 api-client + 10 across 5 apps) plus 7 service tests = 29 total green, 0 failures, 0 skipped
  - pnpm -r --if-present build emits dist/index.{js,d.ts,d.ts.map} for the 3 web packages and dist/index.html + dist/assets/* (~232 KB JS bundle each) for the 5 apps; services emit dist/server.{js,d.ts}
  - Plan-stated `pnpm -w build` does NOT work (root has no `build` script, only `build:services`). Corrected gate uses `pnpm -r --if-present <script>` which silently skips workspaces without that script. Logged as Past Mistake in CLAUDE.md.
  - Prototype unchanged: wc -l index.html=617 app.js=1961 styles.css=2036 (matches pre-task baseline); node --check app.js passes
  - CLAUDE.md = 217 lines (under 300-line global budget)
  - feat/web-monorepo-scaffold merged into main; 4 conflicts resolved (CLAUDE.md, docs/env.md, docs/plans/.../web-monorepo-scaffold.md, pnpm-lock.yaml). Lockfile regenerated by pnpm install. Zero remaining unresolved conflicts (`git diff --name-only --diff-filter=U` empty).
next_actions:
  - Follow-up: workflow-svc → hydrax-adapter HTTP wire-up at lifecycle events (railsclient still constructed but unused at handler layer; main.go line `_ = rails`)
  - Follow-up: pagination UI controls on /products (backend supports limit/offset; UI ships first 50 only)
  - Code-side: auth slice 2b (magic-link enrollment, real production bootstrap path) OR slice 2d (portal UI for passkeys) — slice 2a server substrate landed by parallel session (commits d673e17 → 3a27511)
  - Code-side: workflow lifecycle for short-duration credit (Tier 1 item 4 — Q3; separate plan)
  - User-side §14 unblocks (still open) — Q1 forward HydraX engagement note (paste-ready cover email at §11), Q3 accept/override short-duration credit, Q4 source design partner per persona shortlist, Q7 walk pricing options through commercial owner. See respective docs/plans/2026-04-25-q*.md for action checklists.
  - Code-side: market-data-svc Phase 6 (BFF integration) — stub fan-out from bff to /v1/quotes etc. once a portal needs it
  - Deploy: provision Railway Postgres addon for workflow-svc and run runbook in docs/plans/2026-04-25-persistence-foundation.md (gated on user action)
  - Manual smoke: auth slice 2a browser smoke (register → auth → whoami) — CLI cannot perform, user-only
next_recommended_slice: User picks — (a) act on Q1/Q3/Q4/Q7 unblock-prep drafts (highest leverage, no code change), (b) auth slice 2b magic-link bootstrap (small-medium plan), (c) workflow-svc → hydrax-adapter wire-up at handler layer (small), (d) Q3 short-duration credit lifecycle, (e) pagination UI on /products.
roadmap_alignment:
  prototype_sequence:
    - done — static landing + command-surface preview
    - done — operational workspace shell (sidebar, panels, state banners)
    - done — Orders workbench drill-down (pattern foundation)
    - done — cross-cutting fixture consistency + derived counts
    - done — workspace state persistence (hydrax.workspace.v1)
    - done — Venues panel drill-down (2026-04-24)
    - done — Risk panel review-queue interaction (2026-04-24)
    - done — Positions panel drill-down (2026-04-24)
    - done — Interactive Settings panel with live preferences (2026-04-24)
    - done — Workspace search filter across all panels (2026-04-24)
    - done — Cross-panel venue linking (2026-04-24)
    - done — Notification toasts for cross-panel jumps (2026-04-24)
    - done — Keyboard shortcuts for panel switching (2026-04-24)
    - done — Activity log panel (2026-04-24)
    - done — Activity log persistence (2026-04-24)
    - done — Responsive sidebar collapse (2026-04-24)
    - done — Mobile toolbar responsive pass (2026-04-24)
    - done — Topbar responsive collapse (2026-04-24)
    - done — Sortable table columns (2026-04-24)
    - done — Railway static-site deploy live at hydrax-prototype-production.up.railway.app (2026-04-24)
    - done — Panel-aware filter chips (2026-04-24, corrected: fixtures inlined, 4-file constraint restored)
    - done — Per-panel empty-state filter messaging (2026-04-24)
    - done — Shared drill-down primitive with close button (2026-04-24)
    - done — Contextual breadcrumb trail (2026-04-25)
    - done — Clickable breadcrumb segments (2026-04-25)
    - done — Market data source decision + adapter plan (docs/plans/2026-04-25-market-data-adapter.md, 2026-04-25)
    - paused — prototype slices on hold; /proceed pass complete pending Item C (v1 scaffolding) approval (2026-04-25)
    - next — Item C user approval OR user-picked prototype slice (Esc-to-deselect, breadcrumb history, alternative)
  v1_build_dependencies:
    - prd-v2 §14 Q1 — HydraX API surface (deferred-not-resolved for v1; mocked behind services/hydrax-adapter interface — see CLAUDE.md Decisions (Recent))
    - prd-v2 §14 Q3 — first product template (MMF vs short-duration credit vs treasury-equivalent)
    - prd-v2 §14 Q4 — first tenant design partner
    - prd-v2 §14 Q7 — pricing commitment
  post_v1: see prd-v2 §12 (v2 extends the wedge; v3 platform expansion)
blockers:
  - v1 build still gated by prd-v2 §14 Q3, Q4, Q7. Q1 (HydraX rails) is now scoped-around via mock services/hydrax-adapter interface (see CLAUDE.md Decisions (Recent)), no longer a hard blocker. Prototype slices continue in parallel.
verification_log:
  - "2026-04-26 — Canton interview slide deck (Bold Signal preset, 14 slides, single-file HTML, no commits): docs/demo/canton-interview.html written (1014 lines, 38 KB) plus plan at docs/plans/2026-04-26-canton-interview-deck.md (101 lines). Pairs with prior docs slice (docs/architecture.md + docs/example-subscription-flow.md + README.md). Style — Bold Signal: charcoal #0A0A0B + hot orange #FF6B35 + Archivo Black + Space Grotesk + JetBrains Mono; oversized section-num watermarks per slide (14 of 14 in place); CSS vars for theme swap. Slides — 00 cover, 01 the split (Canton owns/hydrax-app owns thesis), 02 Canton-does-three-things, 03 components, 04 public/traditional/Canton 3-way comparison, 05 where-Canton-stops gap thesis, 06 three-plane diagram, 07 per-service Canton-can't-do-X table, 08 Daml spike code snippet, 09 8-step end-to-end trace, 10 wired-today status grid, 11 trade-offs, 12 what's-next roadmap, 13 closing thesis. Verification — slide count = dot count = 14; viewport rules present (height:100vh, height:100dvh, overflow:hidden, prefers-reduced-motion); HTML structure clean via python3 html.parser (every tag balances; section/div/ul/table balance verified); 0 emoji (LC_ALL=C grep -P emoji ranges returns no hits); short-viewport breakpoints at 700/600/500 px present; per-visual-unit density under 6 bullets/cards (false-positive density flags on slides 01/04/10 because totals split across 2/3/4 cards). Navigation — keyboard (←/→/space/PgUp/PgDn/Home/End), mouse wheel throttled at 700ms, touch swipe, dot click, IntersectionObserver reveal. Out-of-scope honored — no service code, no portal, no prototype, no nano-banana, no deploy. User context: ECC `frontend-slides` skill invoked per CLAUDE.md required-skill rule for UI work."
  - 2026-04-26 — workflow-lifecycle-http slice complete (6 commits be9fe27 bbc171f c2df025 e066107 1cdfa8f 9e62ff7 plus 2 follow-ups fd83afd 9ee1234). End-to-end POST /v1/products/{id}/transition wired workflow-svc -> bff -> api-client -> issuer-portal. workflow-svc per-svc go vet/test green (10 handler tests, 7 repo tests). bff pnpm typecheck/test green (44 tests, 7 in products/proxy + 13 in server.test). web pnpm -r typecheck/test/build green (api-client + ui + tenant-theme + 5 apps; issuer-portal 19 tests). Plan at docs/plans/2026-04-25-workflow-lifecycle-http.md. Two-stage review (spec + code-quality) on every commit. Audit emission deferred (own plan). Railway redeploy of workflow-svc still gated on pgx 5.9.2 + alpine 1.22 toolchain decision.
  - 2026-04-23 — manual cross-check of HTML ids vs JS getElementById (all hit)
  - 2026-04-23 — node --check app.js passes
  - 2026-04-23 — styles.css extended by ~148 lines covering the previously unstyled orders workbench classes
  - 2026-04-23 — second pass added unified venues fixture (9 entries), derived nav counts, derived active-orders pill, workspace-status live pill, and localStorage persistence
  - 2026-04-23 — ACP cron prompt (job f1aa332a) rewritten with a four-file consistency gate, runtime sanity gate, and verification-log requirement before claiming progress
  - 2026-04-24 — docs/prd-v2.md written; applies reviewer feedback point-by-point (rename, wedge narrowing, hard boundary, economic case, persona cuts, module in/out table, on-ledger decision rule, commercialization, boardroom metric, gating open questions)
  - 2026-04-24 — roadmap upgraded against prd-v2; Venues panel locked as next slice with docs/plans/2026-04-24-venues-panel-slice.md; STATE.yaml current_focus, next_actions, and roadmap_alignment updated to match
  - 2026-04-24 — Venues panel drill-down implemented across app.js, index.html, styles.css
  - 2026-04-24 — Risk review-queue implemented across index.html, app.js, styles.css
  - 2026-04-24 — Positions panel implemented across index.html, app.js, styles.css
  - 2026-04-24 — Settings panel implemented across index.html (+42 lines), app.js (+96 lines), styles.css (+87 lines)
  - 2026-04-24 — verification: node --check app.js passes; getElementById audit clean (zero missing); CSS class audit clean; wc -l index.html=531 app.js=1598 styles.css=1396; git diff --stat confirms 3 code files + STATE.yaml changed
  - 2026-04-24 — Workspace search: node --check app.js passes; getElementById audit clean; CSS classes workspace-search and search-hidden declared; wc -l index.html=532 app.js=1623 styles.css=1427; git diff --stat confirms 3 code files changed
  - 2026-04-24 — Cross-panel venue linking: node --check app.js passes; data-venue-link event delegation on orderDetail+positionDetail (both in HTML); venue-link CSS class declared with hover+focus states; wc -l index.html=532 app.js=1655 styles.css=1443; git diff --stat confirms 3 code files changed
  - 2026-04-24 — Notification toasts: node --check app.js passes; toastContainer id in HTML matched by getElementById in JS; toast CSS classes (toast-container, toast, toast-out, toast-accent) declared; showToast wired into jumpToOrder+jumpToVenue; wc -l index.html=534 app.js=1677 styles.css=1503; git diff --stat confirms 3 code files changed
  - 2026-04-24 — Keyboard shortcuts: node --check app.js passes; shortcut-hint + shortcut-list CSS classes declared; keyPanelMap wired to setActivePanel; input/select/textarea guard present; wc -l index.html=545 app.js=1691 styles.css=1542; git diff --stat confirms 3 code files changed
  - 2026-04-24 — Activity log panel: node --check app.js passes; getElementById audit clean (zero missing); activityCountPill/activityTableBody/activityEmpty/panel-activity present; activity CSS classes declared; wc -l index.html=584 app.js=1770 styles.css=1603; git diff --stat confirms 3 code files changed
  - 2026-04-24 — Activity log persistence: node --check app.js passes; clearActivityLog id matched in HTML+JS; clear-log-btn CSS class declared; ACTIVITY_STORAGE_KEY wired; wc -l index.html=585 app.js=1803 styles.css=1617; 3 code files changed
  - 2026-04-24 — Responsive sidebar collapse: node --check app.js passes; sidebarToggle id in HTML:242 matched by JS:1766; sidebarBackdrop id in HTML:238 matched by JS:1767; sidebar-toggle, sidebar-open, sidebar-backdrop CSS classes declared; hamburger animation, slide-in transition, backdrop dismiss all wired; wc -l index.html=591 app.js=1837 styles.css=1718; 3 code files changed
  - 2026-04-24 — Mobile toolbar responsive: node --check app.js passes; searchToggle id in HTML:253 matched by JS:1797; search-toggle, is-active, search-expanded CSS classes declared; summary-grid 1-col at 720px; filter-group flex-wrap; wc -l index.html=594 app.js=1851 styles.css=1782; 3 code files changed
  - 2026-04-24 — Topbar responsive: node --check app.js passes; topbarToggle id in HTML:43 matched by JS:1797; topbar-toggle, topbar-toggle-bar, topbar-open CSS classes declared; hamburger bars-to-X animation; nav link auto-close; wc -l index.html=600 app.js=1872 styles.css=1866; 3 code files changed
  - 2026-04-24 — Sortable columns: node --check app.js passes; 21 data-sort-key attrs in HTML matched by getSortValue in JS; CSS th.sortable + .sort-asc + .sort-desc declared; numeric parsing for exposure/pnl/instruments/queueDepth; severity ranked; wc -l index.html=600 app.js=1978 styles.css=1911; 3 code files changed
  - 2026-04-24 — Railway prototype deploy: package.json + railway.json + .gitignore committed as chore(deploy) 2142206; local smoke test HTTP 200 for /, /app.js, /styles.css on serve@14.2.6 port 3457; Railway project hydrax-prototype id 88b25199-06ab-4708-b59f-eea201f88620; deployment id 833a8363-0ae7-4d48-86be-c432ef78d6e6 status SUCCESS; public URL https://hydrax-prototype-production.up.railway.app verified HTTP 200
  - 2026-04-24 — Deploy flow gotcha: Railway auto-deploy on git push NOT wired; every prototype update requires `railway up --detach` from repo root until user connects GitHub source in Railway dashboard
  - 2026-04-24 — Commit-message mismatch logged: commit 78888d3 titled "persist activity log" but diff was sortable columns; root cause = stale auto-context
  - 2026-04-24 — Panel-aware filter chips (original): violated 4-file constraint by introducing data/fixtures.js as a 5th file
  - 2026-04-24 — Panel-aware filter chips (corrected): fixture data folded back into app.js with inline JSDoc types; data/fixtures.js script tag removed from index.html; node --check app.js passes; getElementById audit clean; no fixtures.js references in code files; wc -l index.html=595 app.js=1914 styles.css=1922; git diff --stat confirms 4 project files changed
  - 2026-04-24 — Per-panel empty-state filter messaging: node --check app.js passes; getElementById audit clean; venuesFilterEmpty/positionsFilterEmpty/riskFilterEmpty ids matched in HTML+JS; filter-empty + is-visible + is-hidden CSS classes declared; wc -l index.html=613 app.js=1939 styles.css=1954; git diff --stat confirms 3 code files + STATE.yaml changed
  - 2026-04-24 — Shared drill-down primitive: node --check app.js passes; getElementById audit clean (zero missing); renderDetailCard() shared across all 4 panels; detail-close click handlers wired on orderDetail/venueDetail/riskDetail/positionDetail via event delegation; CSS detail-close class declared with hover+focus states; data-detail-for attrs on all detail shells; dynamic filter chips via renderFilterChips(); wc -l index.html=613 app.js=1894 styles.css=1981; git diff --stat confirms 4 project files changed
  - 2026-04-25 — data/ cleanup: git rm data/fixtures.js (orphaned 436-line duplicate left behind by f69b7b5); data/ directory empty and removed; node --check app.js passes; zero HydraxFixtures or data/fixtures.js references across html/js/css; wc -l index.html=613 app.js=1894 styles.css=1981 unchanged; git diff --stat confirms 1 file deleted + STATE.yaml
  - 2026-04-25 — breadcrumb trail: node --check app.js passes; breadcrumbTrail id in HTML (index.html:254) matched by getElementById in JS; renderBreadcrumb defined at app.js:514 and called at app.js:672 (selectOrder), 963 (selectVenue), 1144 (selectRisk), 1155 (resolveRisk), 1325 (selectPosition), 1459 (setActivePanel); 5 new CSS classes declared (breadcrumb-trail, crumb-root, crumb-sep, crumb-panel, crumb-item) reusing existing eyebrow treatment; python3 http.server serves /, /app.js, /styles.css as HTTP 200 with expected markers; wc -l index.html=617 app.js=1936 styles.css=2016; visual browser walk-through deferred to user; git diff --stat confirms 3 code files + STATE.yaml changed
  - 2026-04-25 — clickable breadcrumb segments: node --check app.js passes; renderBreadcrumb emits is-interactive + role=button + tabindex=0 + aria-label on crumb-panel and crumb-item whenever a drill-down is active; deselectActivePanel() dispatches to the correct select*() for the active panel; click + keydown (Enter/Space) handlers on breadcrumbTrail with .is-interactive target filter; CSS adds cursor/focus-visible/accent-soft hover state via 3 new rule blocks; serve check confirms deselectActivePanel in /app.js and is-interactive in /styles.css; wc -l index.html=617 app.js=1961 styles.css=2036; visual browser click-through deferred to user; git diff --stat confirms 2 code files + STATE.yaml changed
  - 2026-04-25 — hydrax-governance Daml spike (orthogonal to prototype work): Daml SDK 2.9.5 + Temurin JRE 17.0.19 installed user-local (~/.daml, ~/.java); new throwaway package at services/canton-adapter/daml/hydrax-governance/ with GovernanceProposal template (proposer signatory, approvers+observers disclosure, ensure requiredApprovals in (0, len approvers] + unique approvers), Approve/Execute/Reject choices with controller-gated authz, Proposal interface + ProposalView for upgrade seam; daml build exits 0; 5 Daml Scripts (testHappyPath, testUnauthorizedApprover, testDoubleApproval, testRejectBlocksExecute, testInterfaceView) all exit 0 on --ide-ledger; wc -l daml.yaml=8 Governance.daml=83 GovernanceScript.daml=109 README.md=86 docs/env.md=17; 6 commits on main (64fc029..3567836) plus plan bd645b8; Canton reassignment documented as deferred per PRD §15 (single-synchronizer); does NOT change prototype slice roadmap
  - 2026-04-25 — Item B (decisions recorded): CLAUDE.md gained "Decisions (Recent)" section above Open Questions documenting market data source choice (Binance public + market-data-hub) and PRD-v2 §14 Q1 deferral (mocked services/hydrax-adapter); Open Questions header updated from "(PRD §22)" to "(PRD-v2 §14)" matching STATE.yaml roadmap citations; Q1 entry annotated inline with deferral reference; STATE.yaml summary/current_focus/recently_verified/next_actions/next_recommended_slice/blockers/v1_build_dependencies all updated; market-data-hub public URL https://affectionate-consideration-production-f0be.up.railway.app verified HTTP 200; railway whoami confirms Muhammad Naim auth; railway list shows market-data-hub project; railway link to market-data-hub deliberately skipped (README + workspace CLAUDE.md = ground truth); python3 yaml.safe_load(STATE.yaml) passes; CLAUDE.md wc -l within 300-line global budget; git diff --stat will confirm CLAUDE.md + STATE.yaml changed; committed as abd70f0
  - 2026-04-25 — Item D (market data adapter plan written): docs/plans/2026-04-25-market-data-adapter.md created (210 lines, 16126 bytes); 6-phase build sequence (skeleton → hub upstream → Binance upstream → routing+cache+API → observability+Railway → BFF integration deferred); env-var registry references MARKET_DATA_HUB_URL + MARKET_DATA_HUB_TIMEOUT + BINANCE_API_BASE + optional BINANCE_API_KEY (names only, never values); 24 hits across deferred-not-resolved/HydraX rails/hydrax-adapter/market-data-hub/Binance confirms the plan keeps this layer separate from §14 Q1; secret-leak grep returns 0 hits; STATE.yaml summary/current_focus/recently_verified/next_actions/prototype_sequence updated; implementation gated on Item C approval; /proceed-with-claude-recommendation pass for B+D complete; STOP at Item C (needs explicit user approval before any services/ or web/apps/ scaffolding)
  - 2026-04-25 — backend services scaffold plan written: docs/plans/2026-04-25-backend-services-scaffold.md (11 tasks, 8 services); Task 0 foundation (go.work + pnpm-workspace.yaml + tsconfig.base.json + package.json devDeps + .gitignore), Tasks 1-5 Go services (workflow-svc :7001, approval-svc :7002, audit-svc :7003, hydrax-adapter :7004 with MockRails per Decision 2026-04-25, canton-adapter :7005 alongside preserved Daml spike) — each TDD-driven with /healthz, distroless Dockerfile, README; Tasks 6-8 Node/TS services (notify-svc :7101, integration-svc :7102, bff :7103) — each with vitest test, multi-stage Dockerfile, README, bff loads upstream URLs for all 6 upstreams; Task 9 docs/env.md update; Task 10 full Go+TS verification sweep + STATE.yaml update; module path github.com/naimkatiman/hydrax-app/services/<svc>; package names @hydrax/<svc>; ports 7001-7005 Go, 7101-7103 Node; each task ends in one commit (≤8 files, conventional commit message); ralph-loop compatible; self-review passed (spec coverage 8/8, zero placeholders, type consistency on Health() and startServer() identical across same-stack services); user invoked /superpowers:write-plan (deprecated → writing-plans) + /ralph-loop:ralph-loop next
  - 2026-04-25 — backend services scaffold landed (ralph-loop iteration 1): all 11 tasks executed in a single Claude session. Commits b7a2dea (foundation) → 73f2980 (workflow-svc + 5 go.mod stubs) → 0cbdaeb (approval-svc) → 1f93c37 (audit-svc) → 0da835c (hydrax-adapter+MockRails) → 467954f (canton-adapter Go bridge, scoped git add to skip daml/) → de406ce (notify-svc + pnpm-lock) → 5535409 (integration-svc) → 01a1f1d (bff). docs/env.md updated by parallel commit 80efe7b matching the plan exactly. Verification — go vet ./... && go test ./... pass for all 5 Go services (handler tests cached green; MockRails tests green); pnpm -r typecheck && pnpm -r test && pnpm -r build pass for all 3 Node/TS services (3 vitest /healthz tests green; tsc emits dist/). Prototype unchanged (wc -l index.html=617 app.js=1961 styles.css=2036 matches pre-task baseline; node --check app.js passes). canton-adapter/daml/hydrax-governance/ untouched per git status. Largest commit was workflow-svc at 11 files (5 go.mod stubs needed up front so go.work resolves before per-service tests can run); other service commits 6-8 files each, all within the 15-file cap. No emoji, no UI work, no drive-bys.
  - 2026-04-25 — PRD-v2 §14 unblock-prep pass via /proceed-with-claude-recommendation "unblock the blockers". Honest framing — none of Q1/Q3/Q4/Q7 can be unblocked from a code agent; all four require external/business action. Produced three decision-support drafts (Q3 was already drafted earlier today by parallel agent a973653) — each clearly marked as DRAFT/DEFAULT, overridable, with explicit "User actions to actually unblock" section. Commits 5d8cf8c (Q1 HydraX engagement note, 102 lines, 8 specific questions to forward), d7f1f10 (Q4 tenant persona shortlist, 108 lines, 5 qualifying criteria + 3 archetypes recommended A issuer-led), 4c666bc (Q7 pricing options, 121 lines, 4 candidate models recommended D hybrid). No fabricated firm names, no fabricated dollar amounts. Each doc lists precise user actions (forward, source, walk through commercial owner, log in CLAUDE.md). 3 commits, single-concern, ≤1 file each.
  - 2026-04-25 — market-data-svc landed end-to-end (item 1 of /proceed-with-claude-recommendation pass) plus bff /healthz/composite (item 3). Item 2 (web/apps/) and item 4 (workflow→hydrax HTTP) deferred transparently to fresh sessions. Commits — 6df497d (Phase 1 skeleton + symbol registry on :7006), f079507 (Phase 2 hub upstream client with 6 tests against captured live shapes), d7ac68f (Phase 3 Binance public REST client with 8 tests + interval mapping), b07a160 (Phase 4a+b cache+router — generic Cache[T] with TTL+LRU + 4 tests, router 10-case table test), 07c9f6f (Phase 4c /v1 HTTP API on Go 1.22 path patterns + 8 integration tests + OpenAPI 3.0.3 spec), 2091660 (Phase 5 partial — distroless Dockerfile + docs/env.md update, Prometheus deferred), 2af04d8 (item 3 — bff /healthz/composite fans out to all 8 upstreams with 4 vitest cases, status maps to 200/207/503). Verification — go vet ./... && go test ./... pass for market-data-svc (26 tests across 7 packages); pnpm -F @hydrax/bff typecheck && test && build pass (5 tests across 2 files). UpstreamConfig grew from 6 to 8 entries (added CANTON_ADAPTER_URL :7005 and MARKET_DATA_SVC_URL :7006). Divergences from market-data plan documented in commits — stdlib net/http instead of chi, port 7006 not 8080, JSON /healthz matching workspace, 4 plan §"Open questions" all defaulted. 7 commits, all ≤9 files, all conventional, all green.
  - 2026-04-25 — web monorepo Phase 9 (workspace-wide + main-merge). feat/web-monorepo-scaffold merged into main; conflicts resolved in CLAUDE.md (keep both branches' Past Mistakes — main's 4 + scaffold's 1; keep scaffold's "Web Monorepo — Invariants" section at line 191), docs/env.md (keep main's Service env vars + bff URLs + market-data-svc env vars + scaffold's VITE_BFF_URL section), docs/plans/.../web-monorepo-scaffold.md (identical, AA), and pnpm-lock.yaml (regenerated by pnpm install). Settings.local.json relaxed to allow Edit/Write on .web-scaffold worktree path. Corrected verification gates run from main repo root (NOT plan-stated `pnpm -w build` which fails — root has no `build` script): pnpm -r --if-present typecheck clean across 12 workspaces; pnpm -r --if-present test -- --run = 22 web tests (5 tenant-theme + 5 ui + 2 api-client + 10 across 5 apps) + 7 service tests = 29 total green; pnpm -r --if-present build emits dist/{index.js,index.d.ts} for the 3 web packages and dist/index.html + ~232 KB dist/assets/*.js per app for the 5 apps; root prototype files unchanged (index.html=617, app.js=1961, styles.css=2036 line counts hold; node --check app.js passes). CLAUDE.md = 217 lines (under 300). Per-app dev ports reserved 5173-5177. Lucide-only icons via <Icon> wrapper, CSS-variable theming, workspace:* deps. Visual polish + real BFF endpoints + auth out of scope for the scaffold and explicitly deferred under separate plans (frontend-design + taste-skill + design-system + nano-banana for visual; PRD-v2 §14 Q1 unblock for real BFF). Past Mistake added — `pnpm -w build` does not work because root has no `build` script; use `pnpm -r --if-present <script>` form instead.
  - 2026-04-25 — cross-service contracts wired (item 4 + item 6 of /proceed-with-claude-recommendation pass). Items 1-3 (push, branch delete, worktree delete) explicitly halted — push to origin/main triggers Railway prototype redeploy per railway.json, needs explicit "yes push". Items 5/7/8 deferred to fresh sessions per scope. Commits — dbcac05 (hydrax-adapter exposes POST /v1/issue over HTTP, binds MockRails, 4 unit tests), c58a6b2 (workflow-svc internal/railsclient + 4 httptest tests + main.go reads HYDRAX_ADAPTER_URL), bd9bdd7 (bff /v1/market-data/quotes/{symbol} proxy + 5 vitest tests on the proxy + URL-prefix routing in server.ts). Verification — go vet ./... && go test ./... pass for hydrax-adapter (8 tests across 3 packages) and workflow-svc (4 tests across 2 packages); pnpm -F @hydrax/bff typecheck && test && build all green (10 tests across 3 files: server.ts /healthz, healthz/aggregate.ts × 4, marketdata/proxy.ts × 5). Cross-service shape — workflow-svc never imports hydraxrails directly; bff never imports market-data-svc directly. Same client patterns work whether server side is mock (v1) or real (PRD-v2 §14 Q1 unblock). 3 commits, all single-concern, all conventional, all ≤4 files.
  - 2026-04-25 — Dockerfile audit pass (rule application from f548eab post-mortem). Audit script grepped all 6 Go services for the same regression pattern; found workflow-svc had go.sum from parallel pgx work + bare `COPY go.mod ./` Dockerfile — exact f548eab regression latent. Fixed Dockerfile only (commit f9f5f9e). Two further toolchain divergences logged as deferred (NOT fixed in this commit because the fix needs coordinated workspace-wide bump of go.work + go.mod + Dockerfile FROM line which contradicts ab45945's stated convention of bare `go 1.22`): (a) `golang:1.22-alpine` rejects bare `go 1.22` directive, and (b) pgx v5.9.2 declares `go 1.25.0` minimum which alpine 1.22 cannot satisfy. Unblock decision needs user direction — options A bump alpine to 1.25 + go.work + workflow-svc go.mod, B downgrade pgx, C defer until workflow-svc actually deploys. Other 4 services (approval-svc, audit-svc, canton-adapter, hydrax-adapter) have NO go.sum yet (no external deps), so the regression is purely latent — leaving alone. Persisted as feedback memory at `~/.claude/projects/-home-naim--openclaw-workspace-hydrax-app/memory/feedback-go-docker-vs-workspace.md` — enumerates all 3 divergence points and prescribes `GOWORK=off go build` as the 5-second pre-commit gate.
  - 2026-04-25 — /proceed-with-claude-recommendation /subagent-driven-development pass on the 5-item ranked list. Items 1 (§14 unblocks) surfaced read-only — Q1/Q3/Q4/Q7 plan docs already present and clearly marked DRAFT/DEFAULT, only user can advance. Item 4 (workflow→hydrax-adapter HTTP) verified already-done from prior commits c58a6b2 + dbcac05; go vet + go test pass for both services with cached results. Item 5 (Prometheus + slog on market-data-svc) — subagent C built internal/observability with 8 tests, killed mid-debugging on a metrics_test.go 4096-byte buffer truncation; fixed inline (io.ReadAll) and committed as f548eab (8 files, 616+1−). Smoke run on PORT=7016 confirms /metrics emits http_requests_total + http_request_duration_seconds + cache_*/upstream_* and slog records one JSON line per request with request_id. Item 3 (Railway deploy) — subagent B (initial) provisioned new market-data-svc service in hydrax-prototype project (service ID 0b392be6, public URL https://market-data-svc-production.up.railway.app, env vars set), build failed on missing go.sum copy. Fixed as 5e8af07 (Dockerfile COPY go.mod go.sum ./). Subagent B (attempt 2) hit toolchain mismatch (go 1.22 vs 1.22.0 required by golang:1.22-alpine); fixed as 98e0610 (go.mod patch pin). Subagent B (attempt 3) deployed cleanly — build c4a59dcd, 39.48s, runtime go1.22.12. Smoke verified — /healthz 200 JSON envelope; /metrics Prometheus exposition; /v1/fx/EUR/USD 200 with rate=1.17805 from market-data-hub round-trip; /v1/quotes/BTC%2FUSD 502 upstream_unavailable due to Binance HTTP 451 from Railway Singapore region (asia-southeast1-eqsg3a) — service-side error handling correct, geoblock environmental. Observability verified — http_requests_total{path="/v1/quotes/:symbol",status="502"}=2 confirms path-template label collapsing works in production. Item 2 (investor-portal /health route) — subagent A landed commit 1 (7114fcc feat(web/api-client) — adds CompositeHealth/UpstreamHealth types + useGetHealthzCompositeQuery), then went silent on commit 2; finished inline as 52ff8e9 (App.tsx adds /health route + sidebar Link, HealthRoute.tsx renders 9-tile grid with lucide CheckCircle2/AlertCircle/XCircle status icons + 5s polling + Refresh now button, HealthRoute.test.tsx 3 vitest cases asserting 9 tiles render + status colors + error envelope). Verification — pnpm -F investor-portal typecheck clean, 5/5 vitest passing (2 App + 3 HealthRoute), pnpm -F investor-portal build emits 243.74 KB bundle (was ~232 KB before — 11 KB delta from new lucide icons + RTK polling logic). Multi-agent race avoided — git add scoped to specific paths each commit, parallel persistence-foundation agent landed alongside without conflict (their commits e3e36c0 → c72703a). Final commit log on main has 9 new commits this session. STATE.yaml summary refreshed; CLAUDE.md untouched; deploy report at /tmp/market-data-svc-deploy-report.md (untracked).
  - "2026-04-25 — persistence foundation slice: 10 commits land Postgres schema (5 MVP tables) + workflow-svc internal/db, internal/products, internal/handlers, products CRUD endpoints (POST + GET); pgx v5 driver via database/sql; tests run against docker-compose Postgres on :5433 with txn-rollback isolation; cmd/server reads DATABASE_URL with 3s startup ping (fail-fast on bad DSN), DSN credentials masked via url.URL.Redacted; railsclient still imported with placeholder use; pre-existing parallel-session prometheus middleware (f548eab) and visual-polish plan (untracked) left untouched. Railway provisioning runbook appended to plan doc as user-action follow-up. Verification — go vet ./... silent per service; go test ./... green for workflow-svc (db, products, handlers, railsclient, all 12 tests); end-to-end smoke confirmed via curl POST 201 + GET 200 round-trip with matching JSON. Commits: 74ca260, e3e36c0, ab45945, bc2b135, 315fd5c, c72703a, 138db5b, a80fd6e, 897d480 (plus 7114fcc api-client prereq). Plan: docs/plans/2026-04-25-persistence-foundation.md"
  - "2026-04-25 — persistence foundation Task 7 closes the BFF-side wire end-to-end. Commits — 90a4df2 (services/bff/src/products/proxy.ts with fetchProduct + createProduct + ProductsUpstreamError preserving upstream HTTP status, 4 vitest cases via fakeFetch; server.ts adds POST /v1/products and GET /v1/products/{id} routes mirroring marketdata-proxy shape; 405 guard moved to fall-through), 31f0aa6 (fix — GET /v1/products/{id} route now gates on req.method === 'GET' to restore 405 behavior on non-GET methods), ab3d7a4 (cross-slice review hardening — 64 KiB body cap on POST returns 413; 500 paths on /v1/products + /v1/market-data/quotes/ no longer echo upstream err.message, real error to console.error and client gets static 'an internal error occurred'; new server.test.ts case asserts DELETE /v1/products/abc returns 405 to lock in the 31f0aa6 fix). Verification — pnpm -F @hydrax/bff typecheck clean; pnpm test -- --run 15 tests passing across 4 files (server × 2, healthz/aggregate × 4, marketdata/proxy × 5, products/proxy × 4); pnpm build emits dist/ cleanly. Cross-slice final review verdict — SPEC_COMPLIANT + APPROVED + COHERENT. Slice complete: portal → bff → workflow-svc → Postgres round-trip is wired and tested at every boundary. Plan: docs/plans/2026-04-25-persistence-foundation.md"
  - "2026-04-25 — audit-svc persistence slice: 5 commits land Postgres-backed audit log (POST + GET /v1/audit/events). Commits — 2929a82 (services/audit-svc/internal/db with pgx via database/sql, mirror of workflow-svc), de9cb80 (audit.Event + EventInput rewrite to align with audit_events schema columns), e4eaff9 (Append + ListByResource repo with txn-rollback test isolation; multi-row ordering test loosened to membership only because all rows in one Tx share NOW() — production ORDER BY guarantee verifiable only across separate Txs), 148b296 (HTTP handlers — 64 KiB body cap, error masking, 9 vitest-equivalent cases including 405 + missing fields + oversize body), 08a2519 (cmd/server DATABASE_URL gate — if unset, only /healthz served; if set, ping(3s) then route POST+GET via single mux fan-out, 405 for other verbs). Verification — go vet ./... silent; go test ./... = 12 audit-svc tests + workflow-svc 12 tests still green (no regression); end-to-end smoke confirmed via curl POST 201 → GET returns 1-element array → DELETE returns 405. Plan: docs/plans/2026-04-25-audit-svc-persistence.md (a1fc371). Out-of-scope (anti-scope honored): cross-service wire from workflow-svc, BFF proxy, pagination, soft-delete."
  - "2026-04-25 — product lifecycle state machine slice (pivoted): 1 commit (8eb8336) lands services/workflow-svc/internal/lifecycle as a pure-functional state-machine package — 5 schema-aligned State constants (StatePending/Approved/Active/Matured/Cancelled), Transition(from, to) returning ErrInvalidTransition for invalid edges/self-loops/unknown states, AllowedNext returning successors, IsTerminal flagging matured+cancelled as sinks. 7 table-driven tests cover constants-match-schema, all valid edges, all invalid edges (incl. terminals + rewinds), self-loops, unknowns, AllowedNext membership, IsTerminal correctness. Pivot rationale (per advisor): parallel session committed to services/workflow-svc/cmd/server/main.go and services/bff at 2-min intervals during slice 1; lifecycle as brand-new subdirectory has zero collision risk vs touching the contested files. HTTP transition endpoint + repo.UpdateStatus deferred to follow-up slice once parallel work settles. Verification — go vet ./... silent; go test ./... = 7 lifecycle + 12 audit-svc + ALL workflow-svc packages green (db, products, handlers, lifecycle, railsclient, subscriptions). Plan: docs/plans/2026-04-25-product-lifecycle-state-machine.md."
  - "2026-04-25 — BFF approvals + audit proxies + portal pages slice complete (14 commits, plan docs/plans/2026-04-25-bff-approvals-audit-and-portal-pages.md). Phase A (audit, 3 commits): 37593a6 bff/src/audit/proxy.ts + 4 vitest cases mirroring products/subscriptions canonical shape, e90548f wires POST + GET /v1/audit/events into bff/src/server.ts (POST before req.method!=='GET' short-circuit, GET after) plus 1 405 test, 60fcd03 docs/env.md AUDIT_SVC_URL Consumed-by note. Phase B (approvals, 6 commits): 8de31dc approval-svc/internal/approvals adds Approval+ApprovalInput+DecideInput types + MemRepo (sync.RWMutex+map) + IsNotFound + 7 table tests, c7c3d7d adds Append/Get/ListPending/Decide HTTP handlers (64 KiB body cap, 4xx/405/413/404 error mapping) + 9 httptest cases, d3e5e10 cmd/server/main.go wires repo + routeCollection (POST /v1/approvals, GET pending) + routeItem (/{id} GET, /{id}/decide POST), 617ebf6 bff/src/approvals/proxy.ts adds Approval/CreateApprovalInput/DecideApprovalInput + ApprovalsUpstreamError + listPendingApprovals/fetchApproval/createApproval/decideApproval + 5 vitest cases, 29b058a bff/src/server.ts wires /v1/approvals routes (POST + /decide before short-circuit, GET list and GET /{id} after) + 1 405 test, bc05a01 docs/env.md APPROVAL_SVC_URL Consumed-by note. Phase C (portal pages, 4 commits + 1 hotfix): 5119b0d api-client/src/api.ts adds 6 types (AuditEvent, ListEventsArgs, Approval, ApprovalStatus, DecideApprovalArgs, Subscription) + 4 endpoints (listAuditEvents, listPendingApprovals, decideApproval, getSubscription) + hook re-exports, 416e753 hotfix re-exports the 4 hooks + 6 types from api-client/src/index.ts (root cause: package main is dist/index.js compiled from src/index.ts, src/api.ts symbols invisible to consumers without explicit re-export — caught by the C.2 subagent during typecheck), 0ca044b ops-console /audit search page (3 form inputs + Search button + Result list, useListAuditEventsQuery with skip-until-submitted) + 3 vitest, effed03 distributor-portal /approvals queue (lists pending, Approve/Reject Buttons via useDecideApprovalMutation + refetch) + 2 vitest + CheckSquare nav entry, f09ef27 investor-portal /subscriptions lookup page (input + Lookup + detail card, useGetSubscriptionQuery with skip until submitted) + 2 vitest + FileSignature inline nav Link. Verification — pnpm -r typecheck clean across 12 workspaces; pnpm -r test (vitest) = 144 tests passing across 11 workspaces (services/integration-svc 1, services/notify-svc 1, services/bff 38, web/packages/api-client 6, web/packages/tenant-theme 7, web/packages/ui 40, web/apps/admin 8, investor-portal 7, distributor-portal 10, issuer-portal 15, ops-console 11); pnpm -r build emits dist/ for every workspace; approval-svc go vet+go test green for internal/approvals (7) + internal/handlers (10 = 9 approvals + 1 health); audit-svc internal/handlers needs DATABASE_URL (pre-existing integration test pattern from commit 148b296, untouched in this slice). 3 fetch-mock fix-ups discovered along the way: (a) jsonResponse helper must include Headers + text() + clone() for fetchBaseQuery to round-trip — bare {ok,status,json} fails third-party JSON parse, (b) fetchBaseQuery passes a single Request object to fetch (not (url, init)) so calls assertions iterate mock.calls and read method from either init or request, (c) Test response shapes must match exact BFF JSON (e.g. fileds null vs missing). Anti-scope honored — no Postgres approvals table, no /v1/subscriptions list endpoint, no auth wiring, no emoji."
  - "2026-04-25 — Task B.3 absorption into 78da9a4 (history smell — recorded only, no rewrite). Subscriptions-and-products-form plan (docs/plans/2026-04-25-subscriptions-and-products-form.md) Task B.3 was supposed to ship as its own commit titled `feat(web/issuer): wire ProductDetailRoute + sidebar nav` covering 4 files: web/apps/issuer-portal/src/routes/ProductDetailRoute.tsx (new), web/apps/issuer-portal/src/routes/ProductDetailRoute.test.tsx (new), web/apps/issuer-portal/src/App.tsx (route registration), web/apps/issuer-portal/src/components/IssuerSidebar.tsx (PackagePlus nav entry). Instead, those 4 files got absorbed into commit 78da9a4 titled `feat(web/admin): polished Home` because the admin-portal subagent ran `git add web/apps/admin/` while a parallel B.3 session had already staged its own files. Path-scoped `git add` does not unstage unrelated index entries. The B.3 code is correct and verified (8/8 issuer-portal tests passing, including 3 new ProductDetailRoute tests); the smell is purely commit attribution. Decision (option 2): record here, no `git revert` + recommit, no `git rebase -i`. Rationale: history rewrite risks further collision with parallel sessions and the work itself is sound. Plan reference: B.3 in docs/plans/2026-04-25-subscriptions-and-products-form.md ↔ commit 78da9a4."
  - "2026-04-26 — investor-portal visual-polish slice complete (plan docs/plans/2026-04-25-investor-portal-polish.md). 2 commits — 0c1d6af (feat(web/ui): add investor-portal empty-state hero (nano-banana) — investor-empty-state.jpg 140.86 KB doughnut/pie chart with one cyan-accented slice, +metadata in assets-meta.json, +ASSETS entry in assets.test.ts), 751d1a6 (feat(web/investor-portal): polished Home with NAV, stat tiles, empty state — InvestorSidebar with 7 NAV items, InvestorTopBar with Avatar+notifications, routes/HomeRoute.tsx with 3 stat tiles + EmptyState, App.tsx switches to AppShell brand+sidebar+topbar slots; existing HealthRoute + SubscriptionsRoute moved into routes/ subdir to mirror polished portal layout). Verification — pnpm -F @hydrax/investor-portal typecheck clean; vitest -- --run = 13 tests across 4 files green (4 App + 4 HomeRoute + 3 HealthRoute + 2 SubscriptionsRoute); pnpm -F @hydrax/investor-portal build = vite emits dist/index.html + dist/assets/investor-empty-state-BZtwet16.jpg (140.86 KB) + dist/assets/index-CTM5Lyrg.js (260.61 KB); pnpm -F @hydrax/ui --filter @hydrax/tenant-theme --filter @hydrax/api-client build clean. Workspace-wide pnpm -r typecheck clean across 11 workspaces; pnpm -r test = 150 tests passing (bff 38 + api-client 6 + tenant-theme 7 + ui 40 + admin 8 + distributor 10 + investor 13 + issuer 15 + ops-console 11 + integration-svc 1 + notify-svc 1). pnpm -r --if-present build OOM-killed under parallel pressure (exit 137); per-workspace builds clean. Five-of-five portals now at uniform polish; CLAUDE.md hero asset inventory bullet refreshed."
  - "2026-04-26 — Canton demo docs slice (interview-framing artifacts, no code change): 4 new markdown files written, 0 commits. Plan: docs/plans/2026-04-26-canton-demo-docs.md (77 lines). Files — docs/architecture.md (134 lines, 3-plane diagram + per-service Canton-can't-do-X justification + status snapshot grounded in real commits), docs/example-subscription-flow.md (104 lines, 8-step trace investor → bff → workflow-svc → approval-svc → hydrax-adapter → Daml spike → audit-svc → portals with real file paths and honest wired-vs-mocked call-outs at every step), README.md (138 lines, root entry point opening with 'Canton owns X, hydrax-app owns Y'). Verification — wc -l = 77+134+104+138 = 453 total markdown lines; 27/28 cited file paths resolve via test -e (1 path corrected mid-flight: GovernanceScript.daml lives at daml/Test/ not daml/); zero emoji across all 4 docs (LC_ALL=C grep -P '[\\x{1F300}-\\x{1FAFF}\\x{2600}-\\x{27BF}\\x{1F000}-\\x{1F2FF}]' returns 0 hits); README cross-link count to new docs = 2 (architecture + subscription-flow); STATE.yaml still parses via python3 yaml.safe_load. Out of scope and not touched: services/, web/, db/, the existing prototype, docs/genericresponse.md, docs/homework.md, docs/prd*.md. Anti-scope honored — no new dependencies, no schema, no commits. User context: prep for Canton homework submission and interview demo, deliberate framing as 'Canton primer that ends with the layer above it'."
  - "2026-04-25 — portal-polish slice (distributor-portal + ops-console + admin): 4 commits land the same dark-institutional Home pattern that issuer-portal received earlier today. Commits — b14ec8e (3 nano-banana hero JPEGs at 1200x896 — distributor hub-and-spokes 366 KB, ops-console gauge dials 408 KB, admin key+permission-tree 344 KB; assets-meta.json extended; assets.test.ts asserts every shipped hero is registered), da4497b (distributor-portal — DistributorSidebar 7 NAV items, DistributorTopBar with search+notifications+Avatar, HomeRoute with 3 stat tiles + EmptyState; 8/8 tests green), 719e327 (ops-console — OpsSidebar with Workflows/SLAs/Incidents/Health/Audit, OpsTopBar, HomeRoute with Active workflows / SLAs at risk / Open incidents tiles; 8/8 green), 78da9a4 (admin — AdminSidebar with Tenants/Users/Roles/Audit log/Integrations, AdminTopBar, HomeRoute with Active tenants / Users at risk / Audit events today; 8/8 green; commit accidentally bundled 4 issuer-portal files from a parallel session that had ProductDetailRoute staged at the moment of git commit — git index race, documented as new Past Mistake in CLAUDE.md). Workspace verification — pnpm -r typecheck clean across 11 workspaces; pnpm -r test = 118 tests passing (bff 27, tenant-theme 7, ui 40, distributor 8, admin 8, investor 5, issuer 15, ops 8); pnpm -r build = 5 apps emit dist/ with hero JPEGs bundled. CLAUDE.md extended with hero asset inventory + AppShell-banner-collision rule + git-add-staging-race past-mistake. 4 of 5 portals at uniform polish; investor-portal keeps the upstream-health grid Home as designed. Plan: docs/plans/2026-04-25-portal-polish-distributor-ops-admin.md (committed alongside this entry)."
  - "2026-04-25 — auth-foundation slice 1: 0002 migration applied; integration-svc 25/25 tests (token 7 + repo 7 + handlers 10 + server 1) green against local Postgres; bff 57/57 tests green (proxy 5 + middleware 7 + existing routes updated for bearer + 401-no-bearer test); api-client 12/12 tests green (auth 6 + existing 6); pnpm -r typecheck/test/build green workspace-wide; smoke verified login → whoami → logout → 401 round-trip; AUTH_DEV_LOGIN gating proven (404 when unset). Slice 2 (passkeys vs OIDC) is the next decision point — own plan, multi-session."
  - "2026-04-26 — parallel-dispatch session: 11 commits land issuer-portal /products list end-to-end + audit emission wiring + disabled-state test guard. Two agents dispatched in parallel via /superpowers:dispatching-parallel-agents (Agent A audit emission, Agent B issuer-portal list) collided on services/workflow-svc/internal/handlers/products.go + cmd/server/main.go exactly as the 2026-04-25 working-tree-mutation past-mistake warned. Recovery via stash@{0} (containing both agents' coherent union of workflow-svc work) — split into 2 single-concern commits. Final commit set — Agent A: 304fecc (plan), 98a688d (auditclient package + 4 httptest tests). Agent B: 3e30212 (plan), b474485 (bff listProducts proxy + 4 vitest), 53126ad (api-client useListProductsQuery + barrel re-export), f042ffc (issuer-portal /products route + 4 vitest), 03b8727 (plan amendment marking workflow-svc list deferred). Recovery commits in main session: fcf464a (workflow-svc GET /v1/products List handler + repo.List + querier.QueryContext + route reg + 6 httptest cases incl. 200-with-rows, 200-empty, 400-missing-tenant, 400-bad-limit, 405-non-GET, 200-next-offset-when-page-full + new seedTenant helper), 30cf7b2 (Transition signature now takes AuditEmitter interface, emits product.transitioned on 2xx via derived 2s context, recordingEmitter test double, 6 emit-related test cases incl. happy-path-asserts-call, 422/404/400-no-emit, emit-failure-still-200, nil-emitter-still-200; main.go reads AUDIT_SVC_URL and constructs auditclient when set), 15b579e (docs/env.md AUDIT_SVC_URL row extended with workflow-svc as second consumer), 7b2f9c7 (web/issuer-portal disabled={isLoading} test guard via pending-forever fetch mock). Verification — workflow-svc go vet ./... silent, go test ./... = handlers package 18 tests green (was 12, +6 List + +6 emit), all other packages cached green. pnpm -r --if-present typecheck clean across 11 workspaces with test scripts (12 inc admin). pnpm -r --if-present test -- --run = 257 tests passing across 11 workspaces (bff 73, integration-svc 58, ui 40, issuer-portal 24, investor-portal 13, api-client 12, ops-console 11, distributor-portal 10, admin 8, tenant-theme 7, notify-svc 1; needs DATABASE_URL + INTEGRATION_SVC_DATABASE_URL set for the third-session passkey integration tests in integration-svc). pnpm -r --if-present build clean (5 apps emit dist/ with hero JPEGs bundled, 3 packages emit dist/index.{js,d.ts}, 3 services emit dist/server.{js,d.ts}). Per-service Go gate green for all 5 services (workflow-svc, approval-svc, audit-svc, hydrax-adapter, canton-adapter). E2E smoke deferred (handler tests cover all cases). Plans: docs/plans/2026-04-26-audit-emission-on-transition.md (Agent A's, 304fecc), docs/plans/2026-04-26-issuer-portal-products-list.md (Agent B's, 3e30212 + 03b8727 amendment). Stash dropped after recovery validated. Two follow-ups closed (audit emission + disabled-state test); UI lifecycle polish remains as own plan gated on /frontend-design + /taste-skill + /design-system."
  - "2026-04-26 — auth slice 2a (passkeys, server substrate) implementation complete. 5 commits — d673e17 (db migration 0003 user_passkeys: BYTEA credential_id unique, BIGINT counter, transports text[]), 775fd87 (integration-svc passkey-config + challenge-store LRU + Passkeys repo with counter monotonicity guard incl. Apple-zero case; @simplewebauthn/server@13.3.0; 5+6+9 tests), fc05924 (integration-svc passkey-handlers POST options/verify x2 with library-mocked vitest + server.ts wires mountPasskeyRoutes alongside mountAuthRoutes; 13 handler tests; total integration-svc 58/58), 3c45e90 (bff passkey-proxy module + 4 unprotected routes in server.ts proxying to integration-svc; reuses AuthUpstreamError; extends startMockIntegrationSvc helper to accept routes map; 7 proxy tests + 4 server passthroughs; total bff 73/73 across 10 test files), plus docs commit for env.md + STATE.yaml. Library-mock testing strategy disclosed in plan; live ceremony coverage deferred to slice 2d Playwright. Plan-doc defects caught and noted: (a) plan said 12 passkey-handler tests, actual ships 13 (regOpts 2 + regVerify 3 + authOpts 4 + authVerify 4); (b) plan's 4 register-route test fixtures used raw bytes both as stored token_hash AND as bearer, but handler does sha256 on bearer — fixed by switching fixtures to slice 1's clear-token + hashToken pattern; (c) plan's afterEach Promise resolver typed against Node's close(callback) signature — fixed with explicit Promise<void>((resolve) => upstream.close(() => resolve())). v13 API (current major) verified compatible with verbatim plan code: registrationInfo.credential.{id,publicKey,counter,transports} + verifyAuthenticationResponse takes credential: WebAuthnCredential, all match. **Manual browser smoke (Task 11 — the slice closure gate per the plan) NOT yet performed; cannot be driven by CLI agent. Slice 2a is not formally closed until user drives register → auth → whoami in a real browser.** Slice 2a is server-side substrate only — production passkey enrollment requires slice 2b (magic-link bootstrap) or slice 2d (portal UI) before AUTH_DEV_LOGIN can come down. Plan: docs/plans/2026-04-25-auth-slice-2a-passkeys-server.md."
  - "2026-04-26 — PRD-v2 §14 unblock-prep — trim outcome (option b chosen). The one-screen action pack briefly written earlier today (docs/plans/2026-04-26-q1q3q4q7-action-pack.md, 160 lines) was deleted on the second /proceed pass per advisor's read that ~130/160 lines duplicated source-draft substance. Only the genuinely-new content (paste-ready Q1 cover email, ~30 lines) was retained — inlined as §11 of docs/plans/2026-04-25-q1-hydrax-engagement-note.md (now 139 lines, was 102). Status tracking remains in each source draft's existing append-section convention (Q1 §10 HydraX response, Q4 §8 Selected design partner, Q7 §10 Selected pricing model, Q3 via CLAUDE.md Decisions (Recent)). Net delta — 0 new files, 1 file deleted, 1 file extended by 37 lines. STATE.yaml next_actions §14 pointer simplified back to source drafts with §11 cover email reference. Verification — pack gone (test -e returns false); Q1 doc gained `## 11.` heading (grep -c = 1); STATE.yaml parses via python3 yaml.safe_load. Reasoning — original pack had two-sources-of-truth maintenance overhead (advisor flag) and violated the 'highest leverage, no code' framing (160 lines of meta-doc to act on existing 478 lines of source drafts). Trim path keeps only the additive piece. Skill — /proceed-with-claude-recommendation second pass; user re-invoked /proceed after the keep-vs-trim choice block, taking the option I had leaned away from (a) and toward the leaner discipline option (b)."
  - "2026-04-26 — product-lifecycle-ui-polish slice closed. 4 commits land the polish (0b8fb77 StatusPill primitive with 5-state config + breathing-dot pulse on active + Circle fallback for unknowns; f6aafd0 Toast + ToastProvider + useToast with 4s auto-dismiss + role=status live region + per-toast timer Map for independent dismissal; 6fd455f Button danger variant outlined treatment using --hydrax-color-danger; 161543b ProductDetailRoute polished — StatusPill replaces 'Status: pending' text, status-specific button labels (Approve / Activate / Mark as matured / Cancel product) with lucide icons, Cancel uses variant=danger, success+failure paths surface as toasts via showToast, transitionState.reset() between toasts so repeat clicks fire fresh feedback). Verification — pnpm -F @hydrax/issuer-portal test = 26/26 across 5 files (was 24, +2 toast paths; ProductDetailRoute itself 10/10); pnpm -F @hydrax/ui test = 52/52 across 12 files (was 40, +12 = StatusPill 5 + Toast 6 + Button danger 1, matches plan prediction); pnpm -F @hydrax/bff test = 73/73 unchanged; pnpm -r typecheck clean across all 11 testable workspaces; pnpm -F @hydrax/ui build emits dist/index.{js,d.ts}; pnpm -F @hydrax/issuer-portal build emits 272.10 KB dist/assets/index-*.js + 348.42 KB hero JPEG. Workspace-wide test gate skipped per OOM (137) under parallel pressure with bff + integration-svc + ui all running together; per-workspace runs all green. integration-svc tests still need INTEGRATION_SVC_DATABASE_URL for passkey integration tests (pre-existing, environmental). Plan: docs/plans/2026-04-26-product-lifecycle-ui-polish.md. Visual smoke deferred to user (cannot be driven by CLI). next_actions[0] (lifecycle UI polish follow-up) trimmed."
  - "2026-04-26 — workflow→hydrax-adapter HTTP wire-up landed + auth slice 2b foundations (Tasks 1+2+3+4 of 11). 3 commits — 0193e11 (workflow-svc: rails issuance fires on pending->approved; handlers.RailsIssuer interface + RailsIssuerFunc adapter mirrors AuditEmitter shape, products.SetRailsProductID stamps the id, best-effort policy logs and continues on rails or stamp failure with rails_product_id null for future reconciliation, issuance fires only on pending->approved (other lifecycle edges do NOT call rails), 4 new handler tests + 2 new repo tests; main.go now constructs the issuer instead of `_ = rails`), f002fe8 (db migration 0004_magic_links: magic_link_tokens table with token_hash BYTEA, expires_at TIMESTAMPTZ, used_at nullable, partial index on expires_at WHERE used_at IS NULL, CASCADE on user_id + tenant_id; applied cleanly via psql, schema verified — 7 columns + 4 indexes + 2 FKs), 9d7a17d (integration-svc magic-link config + rate-limit + repo: loadMagicLinkConfig with bounded validation TTL 60-3600s + rate-limit 1-10/window + window 60-3600s + baseUrl trim, createRateLimit insertion-ordered-Map LRU bucket with sliding window per key, MagicLinks.create + atomic UPDATE-RETURNING consume race-free at DB layer; 22 new tests = 10 config + 5 rate-limit + 7 repo against real Postgres). Verification — per-service Go gate green for all 6 services (workflow-svc, approval-svc, audit-svc, hydrax-adapter, canton-adapter, market-data-svc); pnpm -r typecheck clean across 12 workspaces; pnpm -r test = 293 tests passing (notify-svc 1, api-client 12, bff 73, integration-svc 80 (was 58, +22 from slice 2b foundations), tenant-theme 7, ui 52, admin 8, distributor-portal 10, investor-portal 13, issuer-portal 26, ops-console 11); pnpm -r build clean (3 services dist/server.{js,d.ts} + 3 packages dist/index.{js,d.ts} + 5 apps dist/index.html + dist/assets/index-*.js with hero JPEGs bundled). Slice 2b plan source: docs/plans/2026-04-26-auth-slice-2b-magic-link.md (parallel session wrote it; commit 3613cba). DEFERRED to follow-up sessions: Tasks 5-11 = notify-client, notify-svc email-config + email-handlers + server wire, magic-link-handlers + integration-svc server wire (Task 8), bff magic-link-proxy + 2 unprotected routes, docs/env.md update, CLI end-to-end smoke (the slice closure gate per plan Task 11)."
  - "2026-04-26 — auth slice 2b (magic-link enrollment) closure session: Tasks 5-11 of 11 land the production-bootstrap path. 5 commits in this session — 4f15e78 (integration-svc notify-client: small fetch wrapper around POST /v1/notifications/email throwing NotifyUpstreamError on non-2xx + network failure, 4 tests against in-process mock HTTP), dde87ee (notify-svc email surface: loadEmailConfig with EMAIL_TRANSPORT=console default + noop alternative, mountEmailRoutes with 64 KiB body cap + 405/400/413/202 envelope, server.ts wires the route, 4 config + 6 handler + 1 healthz = 11/11 tests; vitest-version-mismatch typecheck fix folded in via inferred MockInstance shape), e281a09 (integration-svc magic-link-handlers + server wire: POST /v1/auth/magic-link/request always 202 no-leak, best-effort token mint + notify-client send, rate-limited per (tenant_slug,email) at 3/15min, GET /v1/auth/magic-link/consume single-use atomic UPDATE-RETURNING, 11 handler tests against real Postgres + mock notify; integration-svc total 95/95 = 58 prior + 37 magic-link), 37832a0 (bff magic-link-proxy + 2 unprotected routes: proxyMagicLinkRequest + proxyMagicLinkConsume reuses AuthUpstreamError, BFF server.ts mounts both as unprotected (magic-link IS the auth path), 6 proxy tests + 4 server passthroughs; bff total 83/83 was 73; plan said 82 but proxy file ships 6 tests not 5 — minor plan-doc undercount). Test gate fix mid-flight: rate-limiter persisted across magic-link-handlers tests since it's keyed by the same (tenantSlug,email) tuple; reassign opts.rateLimit in afterEach (server callback re-reads opts on each request). Task 11 CLI end-to-end smoke green: seeded ml-smoke tenant + ml@example.test admin user → notify-svc on :7101, integration-svc on :7202 (port 7102 had stale parallel-session process; alt ports avoid disrupting it), bff on :8180 → curl POST /v1/auth/magic-link/request returns 202; notify-svc stdout shows '[notify-svc:email]' envelope with 43-char token URL; curl GET /v1/auth/magic-link/consume returns session token + session JSON (role=admin, tenant_slug=ml-smoke, expires 12h out); curl GET /v1/auth/whoami with bearer returns full session info; re-consume same token returns 401 (single-use enforced); 4 sequential requests show 202/202/202/429 (rate-limit enforced). DB cleaned, dev procs killed. AUTH_DEV_LOGIN=1 untouched (slice 2e removal). Slice 2b is server-side substrate + console transport — production passwordless login still requires slice 2c (SMTP/SES/Resend transport adapter). Total 8 commits in slice 2b including foundations + plan-doc. Plan: docs/plans/2026-04-26-auth-slice-2b-magic-link.md."
  - "2026-04-26 — Subscription lifecycle FSM + Approvals Postgres persistence (single /proceed-with-claude-recommendation pass on two recommendations; auto mode). 7 commits mine + 1 bundled-by-parallel-session: aa750be docs(plans) both plan docs; 8038f66 sublifecycle pure-functions package (5 states pending/approved/allocated/settled/cancelled, post-allocation cancel forbidden by design — reversal is a separate compensating workflow, 7/7 tests green); 1d5a5ae subscriptions repo UpdateStatus with optimistic concurrency (mirror products.UpdateStatus, 4 new Tx-rolled tests); 45f2e9f POST /v1/subscriptions/{id}/transition handler (200/400/404/409/422/500, allowed_next surfaced in response, 7 new handler tests including full pending→approved→allocated→settled walk; no audit/rails emission yet — both deferred); e01c05b db migration 0005_approvals (8 cols + 2 indexes + FKs to tenants CASCADE / users SET NULL, applied to test container via psql -f); 221d5fc memrepo first-decide-wins parity (status='pending' guard + errAlreadyDecided + IsAlreadyDecided + handler 409 mapping, 8/8 MemRepo + 11/11 handler tests); 230bef8 wire approval-svc main.go + handler interface refactor (handlers now take approvals.Repo, main.go branches on DATABASE_URL — Pg with 3s startup ping or MemRepo fallback). Bundled-by-parallel-session: 62d4fe5 'chore(notify-svc): add nodemailer + @types/nodemailer for slice 2c' was authored by a parallel session that ran git add+commit between my git add and git commit, sweeping my staged Phase B Pg-repo files (services/approval-svc/internal/approvals/{pg_repo,pg_repo_test,repo}.go + internal/db/{db,db_test}.go + go.mod + go.sum + Dockerfile) into its commit alongside its actual two-file scope (notify-svc/package.json + pnpm-lock.yaml). Code is correct; commit message is wrong. Same root cause as 2026-04-25 Past Mistake (78da9a4) — adopt mitigation: stage+commit chained in one bash invocation with no time gap (used for 230bef8). Verification — pnpm typecheck unchanged (no TS surface touched); workflow-svc go vet/test green per package; approval-svc 8 PgRepo tests against test DB green; end-to-end Pg persistence smoke verified across server restart (POST /v1/approvals → GET → kill → restart → GET same id returns persisted row); first-decide-wins enforced both at MemRepo and PgRepo (re-decide returns 409 already_decided). Tech debt noted: pgx v5.9.2 transitively requires go 1.25 while both go.mod and Dockerfile pin go 1.22 across approval-svc and workflow-svc; GOWORK=off go build fails for both — in-workspace go.work build is the supported path; toolchain-hygiene unification is a separate slice. Deferred follow-ups: subscription audit emission, rails-adapter on approved→allocated, BFF + portal UI for transitions, approval-svc multi-approver chain, approval webhook back to workflow-svc, approval expiration/SLA tracker, ListPending pagination. Plans: docs/plans/2026-04-26-subscription-lifecycle-fsm.md + docs/plans/2026-04-26-approvals-postgres-persistence.md."
