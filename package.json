{
  "name": "hydrax-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Static HTML/JS/CSS prototype of the HydraX operator console. Prototype-only; v1 services live elsewhere.",
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
