{
  "name": "hydrax-context",
  "version": "0.1.0",
  "private": true,
  "description": "Canton Network homework — cover article, slide deck appendix, 5-minute script. Hosted at hydrax-context on Railway.",
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
